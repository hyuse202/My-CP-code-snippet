// Author hyuse202
// Big Inspired by orzdevinwang
#include<bits/stdc++.h>
#define L(i, j, k) for(int i = (j); i <= (k); ++i)
#define L0(i, j, k) for(int i = (j); i < (k); ++i)
#define ll long long
#define int ll
#define ull unsigned long long
#define siz(a) ((int) (a).size())
#define me(a, x) memset(a, x, sizeof(a))
using namespace std;

template <typename T>
void _print(const T &x, int n){
    For(i, 0, n)
        cerr << x[i] << "  ]"[(i == n) + 1];
    cerr << endl;
}
#define dbg(a,n) cerr << #a << " = ["; _print(a, n);
#define _ << " " <<
#define WA assert(false)
#define db(x) cerr << "[ " << #x << " ]" << " = " << "[ " << x <<" ], "

// Code goes here
const int N = 2e5 + 7;

void Main(){

}

int32_t main(){

#ifndef ONLINE_JUDGE
    freopen("inp.txt", "r", stdin);
#endif
    ios::sync_with_stdio(false);
    cin.tie(0);
    int tc = 1;
    cin >> tc;
    while(tc--)
    Main();
    return 0;
}
